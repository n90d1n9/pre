package tech.kayys.erp.foundation.application.command;

/**
 * Marker interface for a write-intent message: "do something that
 * changes state." Commands are named as imperatives (CreateOrder,
 * ConfirmOrder) and each command has exactly one handler.
 */
public interface Command {
}
